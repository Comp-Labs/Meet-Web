import { extendTheme } from "@mui/joy/styles";

export default extendTheme({
    fontFamily: {
        display: "'Rubik', var(--joy-fontFamily-fallback)",
        body: "'Rubik', var(--joy-fontFamily-fallback)"
    }
});
